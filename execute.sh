#!/bin/bash -l

# Required parameters:
# @raycast.schemaVersion 1
# @raycast.title Bater ponto
# @raycast.mode compact

# Optional parameters:
# @raycast.icon 👊🏻

export NVM_DIR="$HOME/.nvm"
    [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm

npm start